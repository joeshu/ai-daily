name: ai-daily-fetch

on:
  workflow_dispatch:
    inputs:
      clear_cache:
        description: 'Clear existing news-data before fetch'
        required: false
        default: 'false'
  schedule:
    - cron: '0 */2 * * *'

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install dependencies
        run: uv sync

      - name: Prepare config
        env:
          AI_DAILY_CONFIG_JSON: ${{ secrets.AI_DAILY_CONFIG_JSON }}
        run: |
          printf '%s' "$AI_DAILY_CONFIG_JSON" > config.json

      - name: Optionally clear old cache
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.clear_cache == 'true' }}
        run: |
          rm -f news-data/fetch-*.json news-data/push-*.md news-data/notify-*.md
          mkdir -p news-data

      - name: Run fetch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
          JINA_API_KEY: ${{ secrets.JINA_API_KEY }}
        run: uv run python src/main.py fetch

      - name: Commit news-data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f news-data
          git diff --cached --quiet && echo "No changes to commit" && exit 0
          git commit -m "chore: update fetched news data"
          git push
